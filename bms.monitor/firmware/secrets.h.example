// Copy to secrets.h and fill in. secrets.h is gitignored so the
// real values never leave this machine.

#pragma once

static const char* WIFI_SSID = "your-ssid";
static const char* WIFI_PASS = "your-password";
