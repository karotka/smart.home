// Copy this file to secrets.h and fill in the real values.
// secrets.h is gitignored; this template is what we commit.
#ifndef SECRETS_H
#define SECRETS_H

#define WIFI_SSID     "YOUR_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

// OTA flash password — keep a random LAN client from pushing firmware
// to this board over ArduinoOTA.
#define OTA_PASSWORD  "YOUR_OTA_PASSWORD"

#endif
